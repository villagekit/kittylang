# Research

Syntheses of research sweeps, one file per sweep, `YYYYMMDD-<slug>.md`.
Each opens by saying what was asked, which sub-agents ran, and where their
full reports are; the reports live in the shared villagekit `transcripts`
repo checked out as a sibling (`../transcripts`), under `kittylang/`, one
directory per session, archived by the `/transcripts` skill. Syntheses are
dated documents, not items.

This file is the session index.

## Sessions

None yet.
